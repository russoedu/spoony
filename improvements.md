1. Cache Google login so it is not asked every time the app is opened
2. Make the theme more attractive and fun - use colours.svg
3. Fix the viewport to avoid the screen zooming when a field is selected
4. Update the top app logo with the correct one (full logo.svg)
5. When the language is auto detected via browser, make sure it's set in the configurations and displayed when the options are shown
6. Create splash screens (use https://github.com/avadhesh18/iosPWASplash) with the splash.svgMake sure the user goes to the activities editor (configuration) on the first access
7. In the activities editor:
   1. Make the legend fixed in the bottom with a drawer effect to display/hide
   2. Waking spoons can't be edited, removed or added. just display as the first item
   3. Create 3 sections: scale entries (where pain levels is), activities and notes. These groups start after "waking up"
   4. Each section has its own add button that adds a new item to the group
   5. Drag and drop is allowed only in the same group and you can't change a group
      1. The currect action only works for "takes/gives spoons
8. In the main screen
   1. where the etries are added, the activities that give spoons should have the label "gained" instead of "Used"
   2. In the "Other activites", the same, depending on the type, change it from Used/Gained depending on the type