declare module 'ios-pwa-splash' {
  /** Generates iOS PWA splash-screen <link> tags from an icon and background color. */
  export default function iosPWASplash(icon: string, color?: string): void;
}
